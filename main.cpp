#include <iostream>



// CCSDS Telecomand Telemetry Parser
int main()
{
    std::string word;
    while (word != "quit")
    {
        std::cout << "Enter a word: ";
        std::cin >> word; // Reads only one word (stops at space)

        if(word == "camera");
    }
    return 0;
}
